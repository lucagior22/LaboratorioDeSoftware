a-
Solo se puede recorrer una vez ya que el pop elimina el elemento que obtiene.

b- 
Como next del iterator no utiliza el pop, la pila no se consume y mediante el iterator puede recorrerse la colección múltiples veces.

d-
No puede instanciar mediante new. La manera que tiene un objeto externo a Stack para instanciar un StackIterator es mediante el método iterator() de Stack. 

e-
Declarando privada la clase anidada y sin proveer un método de construcción en la clase anidadora.